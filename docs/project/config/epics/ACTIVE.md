# Active Context

## Current

- **Epic:** EPIC-1 — Shell foundation
- **Story:** 1.1 — Repo skeleton + Electron boots
- **File:** [epic-1/story-1.1.md](epic-1/story-1.1.md)
- **Goal:** `npm run dev` opens an Electron window titled "MultiTerm"
- **Modules touched:** repo root, `src/main/`, `src/renderer/`, `src/preload/`, `src/shared/`
- **Status:** [→] In progress
- **Plan reference:** Tasks 1.1, 1.2, 1.3, 1.4 in `docs/plans/2026-04-28-multiterm-mvp.md`

## Next up

1.2 → IPC roundtrip (preload + ping/pong) → user clicks Ping, sees RTT.

## Completed

<!-- Move finished stories here as they close. Format: `[x] Story X.Y — name (commit-sha)` -->

---

## Notes

- Operating mode is **autonomous progression** — work continues without per-step approval, stopping only at user-test checkpoints inside stories.
- After each story closes, advance the pointer above and move the closed story to "Completed".
- `build-log.md` and `codebase.md` are updated at story close (not per-task).
